## media ready

1. Socket initalized on entering the metaverse
2. Create peerid and emit 'media-ready' (`client` to `server`)
3. Request audio/video access
4. Send media streams (P2P), empty if no audio/video access is granted
